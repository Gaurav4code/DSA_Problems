class Solution {
public:
    vector<int> shuffle(vector<int>& nums, int n) {
        
        vector<int> ju;
        for(int x=0 ,y=n;x<n ,y<(2*n);x++,y++){
            ju.push_back(nums[x]);
            ju.push_back(nums[y]);

        }
        
        return ju;
    }
};
