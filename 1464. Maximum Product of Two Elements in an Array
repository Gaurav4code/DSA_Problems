class Solution {
public:
    int maxProduct(vector<int>& nums) {
        
        sort(nums.begin(),nums.end());
        int z=nums.size()-1;
        int y=(nums[z-1]-1)*(nums[z]-1);
        
        return y;
    }
};
